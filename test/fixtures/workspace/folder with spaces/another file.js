// Another test file in a folder with spaces
function handleSpacePath() {
  console.log('This file is in a folder with spaces');
}

// Search test marker
const SPACES_IN_PATH_TEST = 'Found in space path';

module.exports = {
  handleSpacePath,
  SPACES_IN_PATH_TEST
};